# Base site URL used in RSS and canonical links
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Public base URL where your audio files are served from.
# IMPORTANT: RSS enclosures MUST be publicly accessible, not presigned.
PUBLIC_AUDIO_BASE_URL=https://example-cdn.yourdomain.com/audio

# Storage driver for presigning: public | s3 | r2 | bunny
STORAGE_DRIVER=public

# AWS S3 (only if STORAGE_DRIVER=s3)
AWS_S3_REGION=us-east-1
AWS_S3_BUCKET=your-bucket
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Cloudflare R2 (only if STORAGE_DRIVER=r2)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_DOMAIN=https://pub-example.r2.dev

# Bunny (only if STORAGE_DRIVER=bunny)
BUNNY_STORAGE_ZONE=
BUNNY_API_KEY=
BUNNY_REGION=ny
BUNNY_PULL_ZONE_URL=https://your-pull-zone.b-cdn.net

# Views counter (Upstash Redis optional)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Basic protection config
VIEW_COOKIE_TTL_DAYS=3
RATE_LIMIT_MAX=20
RATE_LIMIT_WINDOW_SEC=60
